{% extends 'base.html.twig' %}

{% block body %}

        <div class="container-fluid">
            <div class="row">
                    <div class="col-md-auto">
                        {{ block("sidebar", 'base.html.twig') }}
                    </div>

                <div class="col">
                    <br/>

                     <h2>Stwórz konto nowego pracownika</h2>
                    {{ form(form) }}
                </div>
            </div>
        </div>


{% endblock %}
