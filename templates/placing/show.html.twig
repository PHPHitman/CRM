{% extends ('base.html.twig') %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-auto">
                {{ block("sidebar", "crm/index.html.twig") }}
            </div>
            <div class="col">
                <h1>{{ placing.service }}</h1>
            </div>
        </div>
    </div>
{% endblock %}